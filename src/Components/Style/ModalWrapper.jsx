import styled from "styled-components";

export const ModalWrapper = styled.div`
    width: 800px;
    height: 600px;
    box-shadow: 0 5px 16px rgba(o, 0, 0, 0.2);
    background-color: #fff;
    color: #000;
    display: grid;
    grid-template-columns: 1fr 1fr;
    position: relative;
    z-index: 10;
    border-radius: 12px;
`